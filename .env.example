# ==========================================
# Voice Meeting Spec Generator Configuration
# ==========================================

# --- GitHub Integration (Required) ---
# Create an OAuth App in GitHub Developer Settings to get these.
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# This must match the Authorization callback URL in your GitHub App settings
CALLBACK_URL=http://localhost:8000/auth/github/callback

# --- Security (Required) ---
# A random string used to encrypt sensitive tokens (like GitHub OAuth tokens) in the database.
# You can generate one using: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=change_this_to_a_secure_random_string_32_chars

# --- AI Services (Required) ---
# Token for Hugging Face Inference API (used for Llama-3 summarization & spec generation).
# Get it for free at: https://huggingface.co/settings/tokens
HUGGING_FACE_KEY=hf_your_hugging_face_token_here

# --- Transcription & TTS (Optional / Configurable) ---

# Choose the transcription provider.
# Options: "whisper_local" (Free, runs on device) | "elevenlabs" (Paid API)
TRANSCRIPTION_PROVIDER=whisper_local

# Required ElevenLabs for high-quality Bot voice (TTS).
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Required ONLY if you switch code to use OpenAI models instead of Hugging Face/Local Whisper.
OPENAI_API_KEY=sk-your_openai_api_key_here